apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "service-desk-backend.configMapName" . }}
  labels:
    {{- include "service-desk-backend.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.env | nindent 2 }}
